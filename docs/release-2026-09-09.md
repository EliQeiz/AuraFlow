# AuraFlow release: 2026-09-09

## Deployment

- Public website: https://aura-flow-red.vercel.app
- Workspace: https://aura-flow-red.vercel.app/dashboard
- Owner console: https://aura-flow-red.vercel.app/dashboard/admin
- Rebuild commit: `7e55a6a` on `codex/platform-rebuild`.
- Vercel project: `aura-flow`, scope `elisha-afaris-projects`.
- Initial release deployment: `dpl_F1ptQyQkrkPZG7fwEUAfP1Hw4Z9E` (READY).
- Previous deployment retained for rollback: `dpl_Y7MZKs2KgkPPC2YxZ9hT6EGAwJ4w`.
- Firebase project: `auraflow-eece6`. Firestore rules, Storage rules, and indexes deployed successfully. No customer records were migrated or deleted.
- Vercel explicitly builds Vite with `npm ci`, `npm run build`, and the `dist` output directory.

GitHub used `EliQeiz` through a command-scoped credential helper; commit author and Firebase/Vercel owner were `elishaafari0@gmail.com`. No global account selection or Git identity was changed. Environment files, credentials, emulator data, and the reference-system folders remain excluded from Git/deployment.

## Verified

- Build, TypeScript, lint, and 10 unit/configuration tests passed again before release. Previous local QA passed 9 security groups and 12 browser scenarios.
- Live desktop/mobile public-route smoke passed: rendering, no page runtime exceptions, no horizontal overflow, private-route redirects, template search, mobile preview, and preview-to-login handoff.
- Live HTML/assets and `/api/health` respond successfully; HTTPS security headers are present.
- Production email/password and Google providers are enabled. The public Vercel domain is authorized for Firebase sign-in.
- The owner's verified, enabled Firebase account now has the server-issued `admin: true` claim. A new sign-in is required to refresh an existing session.
- The seven production Firebase web configuration values were reapplied from the validated local AuraFlow configuration without printing their values.

To repeat the non-mutating live route smoke check:

```powershell
$env:PRODUCTION_BASE_URL = 'https://aura-flow-red.vercel.app'
npm run test:production
```

## Outstanding production gates

**Cloud Storage billing is blocked.** Google Cloud rejected the origin-scoped CORS update with HTTP 403: `The billing account for the owning project is disabled in state closed`. Storage rules deployed, but that does not prove file operations are available. The owner must restore an active billing account for this project. Billing/payment settings were not changed.

After billing is restored, configure bucket CORS for `https://aura-flow-red.vercel.app` with GET/HEAD and verify authenticated reference uploads, avatar uploads, and private preview downloads. Preserve any other legitimate CORS entries. Keep Firebase ownership rules enforced; do not make client objects public as a workaround. The pre-release rules and bucket configuration were backed up locally under the ignored `.tools/deployment-backup/` directory.

Interactive Google sign-in, real customer sessions, and production file workflows still require an owner-led acceptance check. Do not use the local emulator demo passwords on this deployed site.

The older Supabase media-processing API is preserved but is not configured in Vercel (`/api/projects/list` returns a missing `SUPABASE_URL` configuration error). This is separate from the rebuilt Firebase workspace. Its media worker must not be advertised as operational.

This release remains a client design and delivery workspace. Operational school/commerce/hospitality runtimes, automatic tenant/domain provisioning, native applications, payments, and the broader security release gates in `rebuild-status.md` are not complete.
